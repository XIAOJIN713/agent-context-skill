# 交接文档（Handoff）：{会话或任务主题}

保持这个文件短小。它只用于帮助新会话（session）继续当前任务，不是背景资料、会议纪要或全项目总结。

推荐文件名：`agent-context/handoff/{topic}-{YYYYMMDD}.md`

- 同一任务继续更新原 handoff。
- 不同任务新建独立 handoff。
- 已完成、重复或过期的 handoff 应标记 obsolete 或删除。
- 不要为同一任务反复创建多个 handoff。

## 当前状态

- 状态：`{当前任务进展到哪里}`
- 最近一次有意义的动作：`{最近改变了什么，或确认了什么}`
- 工作上下文，如相关：`{分支、环境、命令、产物、约束或其他必要信息}`

## 已确定结论

- `{仍会影响后续工作的结论}`
- `{仍会影响后续工作的结论}`

## 卡点 / 待确认

- `{卡点或问题}`：`{解决它需要什么}`
- `{卡点或问题}`：`{解决它需要什么}`

## 下一步

1. `{下一步动作}`
2. `{下一步动作}`
3. `{下一步动作}`

## 关键文件

只列与继续当前任务强相关的关键文件。说明新会话为什么需要读取它们。

- `{路径}`：`{为什么新会话需要读取}`
- `{路径}`：`{为什么新会话需要读取}`
